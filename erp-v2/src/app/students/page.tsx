import { ComingNext } from "@/components/app/coming-next"

export default function Page() {
  return <ComingNext title="นักเรียน" fixes={["F7 สถานะคำนวณจากแพ็กเกจจริง", "C4 คาบคงเหลือใช้ตัวเลขเดียวกันทุกหน้า", "G3 ครู export ไม่ได้"]} />
}
