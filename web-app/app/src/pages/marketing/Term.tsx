import MarketingShell from '../../components/web/MarketingShell'

/* Term (เงื่อนไขการใช้งาน) — โครงไว้รอ */
export default function Term() {
  return <MarketingShell title="เงื่อนไขการใช้งาน (Terms)" />
}
