import MarketingShell from '../../components/web/MarketingShell'

/* Article (บทความ) — โครงไว้รอ (แปลงจาก prototypes/articles.html) */
export default function Article() {
  return <MarketingShell title="บทความ (Article)" />
}
