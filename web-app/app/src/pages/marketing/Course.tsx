import MarketingShell from '../../components/web/MarketingShell'

/* Course (คอร์สเรียน) — โครงไว้รอ (แปลงจาก prototypes/courses.html) */
export default function Course() {
  return <MarketingShell title="คอร์สเรียน (Course)" />
}
