import MarketingShell from '../../components/web/MarketingShell'

/* Grade (ชั้นเรียน) — โครงไว้รอ (แปลงจาก prototypes/grade.html) */
export default function Grade() {
  return <MarketingShell title="ชั้นเรียน (Grade)" />
}
